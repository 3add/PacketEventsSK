test "BackgroundColorPropTest":
    create new fake text display entity at test location for players and store it in {_e}:
        set fake display billboard of the fake entity to center
    assert {_e} is set with "Fake item display entity should be created"
    assert fake display billboard of {_e} is center with "Fake display billboard should be set to center"