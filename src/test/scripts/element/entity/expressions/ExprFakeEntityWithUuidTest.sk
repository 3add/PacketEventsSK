test "ExprFakeEntityWithUuid":
    create new fake player entity at test location for players and store it in {_e}
    assert {_e} is set with "Fake item display entity should be created"
    set {_id} to fake uuid of {_e}
    assert fake entity with uuid {_id} is {_e} with "Syntax should be correct"